# Phase 1: Scraper (monorepo)

The scraper has moved to the Turborepo app **`apps/scraper`**.

- **Setup and usage**: see [apps/scraper/README.md](../../apps/scraper/README.md).
- **Run from repo root**: `npm run scrape -- <work_id> [work_id ...]`

This directory is kept for reference; the implementation lives under `apps/scraper`.
