[project]
name = "parseltongue-logger"
version = "0.1.0"
description = "Shared logging setup for the Parseltongue pipeline"
requires-python = ">=3.11"
dependencies = [
    "rich>=13.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
